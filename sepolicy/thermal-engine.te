#============= thermal-engine ==============
allow thermal-engine socket_device:sock_file { create setattr };
allow thermal-engine sysfs_spmi_dev:dir { read search open };
allow thermal-engine sysfs_spmi_dev:lnk_file read;
